v20 - 7 files, from v17
=======================

You are on v17. These seven bring you fully up to date (v18 + v19 + v20).
Copy all seven - they are interdependent.

  lib/services/alarm_event_log.dart          NEW
  lib/screens/setup_wizard_screen.dart       NEW
  lib/main.dart
  lib/screens/alarm_health_screen.dart
  lib/services/notification_service.dart
  lib/services/foreground_alarm_service.dart
  lib/services/foreground_alarm_manager.dart

No pubspec change. No workflow change. No new package.

=====================================================================
YOUR LOG SETTLED IT
=====================================================================
51 unbroken heartbeats, 21:28 to 22:19, one every minute, no gaps.
  -> the service is alive and healthy. Samsung is NOT freezing it.

  21:31:30  test alarm armed  default sound + exactAllowWhileIdle
  21:32:05  service tick
  21:33:05  service tick
  -> armed without error, app provably awake through the firing moment,
     nothing arrived. Same again with alarmClock at 22:18:46.

CONCLUSION: zonedSchedule does not deliver on this device. Not with
exactAllowWhileIdle, not with alarmClock. It arms, reports success, and the
alarm never arrives. Meanwhile _plugin.show() from the service works perfectly
- your instant test proved that, and the heartbeats prove the service runs.

I have spent six versions trying to fix the path that does not work on your
phone, while the path that does work was running flawlessly the whole time.

=====================================================================
WHAT v20 CHANGES
=====================================================================
THE TEST NOW RUNS DOWN BOTH PATHS.
"Test both paths in 60 seconds" queues one test on the background service and
one on the OS scheduler. The log records which arrived:

    "FIRED test by service"   -> the service path works. This is the one that
                                 carries your prayers.
    (nothing)                 -> the OS scheduler again delivered nothing.

That single line tells you which mechanism your app can rely on.

The service test is delivered by the same 20-second poll that fires prayers,
so it tests the real path rather than a parallel one.

=====================================================================
WHAT THIS MEANS FOR YOUR APP
=====================================================================
If the service test fires and the OS one does not - which your log strongly
predicts - then your prayer alarms are ALREADY being delivered by the service,
and have been all along. The 8:20 Maghrib that worked was the service. The
8:24 that did not was the service running on stale times, which v16 fixed.

The practical consequence: the foreground service must stay alive. That is
what the setup wizard's step 4 is for, and why the heartbeat log matters - a
gap in it is the only thing that can silence your alarms now.

=====================================================================
DO THIS
=====================================================================
1. Apply all seven files. Rebuild. Install.
2. Complete the setup wizard (new on first launch).
3. Alarm health > "Test both paths in 60 seconds". Lock the phone.
4. Send me the log. I want to see which line appears.
5. Then set a prayer 3 minutes ahead and let it run for real.
