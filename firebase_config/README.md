Put your Firebase-downloaded `google-services.json` file directly in
this folder (not inside `android/` — that folder gets regenerated
fresh on every build, so this is the one stable place the workflow
looks for it and copies it into the right spot automatically).
